/**
 * Props for the Manual component.
 *
 * @interface ManualProps
 * @property {function} handleUpdatePumpWaterClick - Callback function to handle the event of updating the pump water.
 */
export interface ManualProps {
    handleUpdatePumpWaterClick: () => void;
}