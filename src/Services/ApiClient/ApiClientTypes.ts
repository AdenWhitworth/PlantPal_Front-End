/**
 * Interface representing an error response object.
 *
 * @interface ErrorResponse
 * @property {string} [message] - An optional error message describing the issue.
 */
export interface ErrorResponse {
    message?: string;
}