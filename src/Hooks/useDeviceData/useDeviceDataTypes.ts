/**
 * Props for the UseDeviceData hook.
 * 
 * @interface UseDeviceDataProps
 * @property {Function} handleLogout - Function to handle user logout.
 */
export interface UseDeviceDataProps {
    handleLogout: () => void;
}